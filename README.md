# Sonda

This repository contains a (currently private) prototype of a
personal tool for monitoring the access-network quality.

## Steps to make it public

- [ ] adapt `README.md` to be like e.g. `npte` or `runtimex`

- [x] add `.github/dependabot.yml`

- [x] add `.github/workflows/go.yml`

- [x] add `man/` with manpage

- [x] add `CLAUDE.md`

