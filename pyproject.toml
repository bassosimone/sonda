[project]
name = "sonda"
version = "0.1.0"
description = "Research tools for exploring sonda measurements"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pandas",
    "plotly",
    "pyarrow",
    "streamlit",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
